{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "oracle": {
    "host": "${ORACLE_HOST}",
    "port": "${ORACLE_PORT}",
    "database": "${ORACLE_DB}",
    "password": "${ORACLE_PASSWORD}",
    "user": "${ORACLE_USER}",
    "connector": "oracle"
  }
}
